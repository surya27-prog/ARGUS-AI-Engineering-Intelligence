[alembic]
# Migrations live in database/migrations at the repo root, not under backend/.
# %(here)s resolves to this file's directory, making the path absolute — a bare
# "../database/migrations" makes Mako reject the template as outside its root.
script_location = %(here)s/../database/migrations
prepend_sys_path = .
path_separator = os
# sqlalchemy.url is set at runtime in env.py from Settings.database_url.

[loggers]
keys = root,sqlalchemy,alembic

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARNING
handlers = console
qualname =

[logger_sqlalchemy]
level = WARNING
handlers =
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers =
qualname = alembic

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
datefmt = %H:%M:%S
