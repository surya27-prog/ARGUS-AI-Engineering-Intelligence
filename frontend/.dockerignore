# node_modules is reinstalled inside the image from the lockfile; copying the
# host's would bring platform-specific binaries for the wrong platform.
node_modules/
.next/
out/
npm-debug.log*

.env
.env.*
